{
    "builds": [{
        "src": "academic_system/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "academic_system/wsgi.py"
        }
    ]
}